/* Streetview Journey transport classifier — isolated route profiles v0.2.0 */
(()=>{
  'use strict';
  if(window.__journeyTransportClassifierInstalled)return;
  window.__journeyTransportClassifierInstalled=true;

  const VERSION='0.2.0-profile-isolation';
  const ROUTE_KEY='streetview:journey-route';
  const CACHE_PREFIX='streetview:transport-classifier:v2:';
  const CACHE_TTL_MS=6*60*60*1000;
  const SAMPLE_POINTS=7,TRANSITION_CONFIRM_FRAMES=3;
  const TYPES=new Set(['CAR','WALK','TRAIL','BIKE','TRAIN','UNKNOWN']);
  const PROFILES=new Set(['ROAD','SIDEWALK','MOUNTAIN','RAIL','BIKE','AUTO','UNKNOWN']);
  const TYPE_PROFILE=Object.freeze({CAR:'ROAD',WALK:'SIDEWALK',TRAIL:'MOUNTAIN',TRAIN:'RAIL',BIKE:'BIKE',UNKNOWN:'UNKNOWN'});
  const PROFILE_TYPE=Object.freeze({ROAD:'CAR',SIDEWALK:'WALK',MOUNTAIN:'TRAIL',RAIL:'TRAIN',BIKE:'BIKE',AUTO:'UNKNOWN',UNKNOWN:'UNKNOWN'});
  const TEST_PROFILE=Object.freeze({road:'ROAD',sidewalk:'SIDEWALK',mountain:'MOUNTAIN',rail:'RAIL'});
  const state={version:VERSION,transportMode:'UNKNOWN',journeyProfile:'AUTO',profileSource:'unresolved',sequenceModes:{},cacheHits:0,requests:0,errors:0,last:null};

  const rad=v=>v*Math.PI/180;
  function selectedRoute(){if(window.__journeySelectedRoute)return window.__journeySelectedRoute;try{return JSON.parse(sessionStorage.getItem(ROUTE_KEY)||'null')}catch{return null}}
  function normalizeProfile(v){const p=String(v||'').trim().toUpperCase();return PROFILES.has(p)?p:'AUTO'}
  function profileFromHints(route,frames){const direct=normalizeProfile(route?.journeyProfile||route?.routeProfile||route?.profile);if(direct!=='AUTO'&&direct!=='UNKNOWN')return{profile:direct,source:route?.profileSource||'route-explicit'};const testId=String(route?.fixedTestRoute?.routeId||route?.destination?.testRouteType||'').toLowerCase();if(TEST_PROFILE[testId])return{profile:TEST_PROFILE[testId],source:'test-route-id'};const genre=String(route?.routeGenre||route?.genre||route?.category||'').toUpperCase();if(['ROAD','CAR','DRIVE','DRIVING'].includes(genre))return{profile:'ROAD',source:'production-genre'};if(['SIDEWALK','WALK','PEDESTRIAN'].includes(genre))return{profile:'SIDEWALK',source:'production-genre'};if(['MOUNTAIN','TRAIL','HIKING'].includes(genre))return{profile:'MOUNTAIN',source:'production-genre'};if(['RAIL','TRAIN'].includes(genre))return{profile:'RAIL',source:'production-genre'};const typeHints=(frames||[]).map(f=>String(f?.transportType||'').toUpperCase()).filter(t=>TYPES.has(t)&&t!=='UNKNOWN');if(typeHints.length){const counts={};for(const t of typeHints)counts[t]=(counts[t]||0)+1;const top=Object.entries(counts).sort((a,b)=>b[1]-a[1])[0]?.[0];if(top)return{profile:TYPE_PROFILE[top]||'AUTO',source:'frame-transport-hint'}}return{profile:'AUTO',source:'classifier'}}
  function setRouteProfile(profile,source,transportMode='UNKNOWN',details=null){profile=normalizeProfile(profile);const route=selectedRoute(),list=window.__journeyStreamState?.frames||route?.frames||[];if(route){route.journeyProfile=profile;route.profileSource=source;route.profileIsolation=true;route.transportMode=transportMode;try{sessionStorage.setItem(ROUTE_KEY,JSON.stringify(route))}catch{}window.__journeySelectedRoute=route}for(const f of list||[])if(f&&typeof f==='object')f.journeyProfile=profile;const changed=state.journeyProfile!==profile||state.profileSource!==source;state.journeyProfile=profile;state.profileSource=source;if(changed){const detail={at:new Date().toISOString(),journeyProfile:profile,profileSource:source,transportMode,details};try{window.dispatchEvent(new CustomEvent('journey-profile-changed',{detail}))}catch{}}return profile}
  function currentProfile(){const route=selectedRoute(),hint=profileFromHints(route,window.__journeyStreamState?.frames||route?.frames||[]);if(hint.profile!=='AUTO')return hint.profile;return state.journeyProfile||'AUTO'}
  function isProfile(profile){return currentProfile()===normalizeProfile(profile)}

  function hasCoords(f){return Number.isFinite(Number(f?.lat))&&Number.isFinite(Number(f?.lng))}
  function distanceMeters(a,b){if(!hasCoords(a)||!hasCoords(b))return NaN;const p1=rad(Number(a.lat)),p2=rad(Number(b.lat)),dp=p2-p1,dl=rad(Number(b.lng)-Number(a.lng)),s=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return 12742000*Math.atan2(Math.sqrt(s),Math.sqrt(Math.max(0,1-s)))}
  function median(values){const a=values.filter(Number.isFinite).sort((x,y)=>x-y);if(!a.length)return NaN;const m=a.length>>1;return a.length&1?a[m]:(a[m-1]+a[m])*.5}
  function timestampMs(v){if(v==null)return NaN;const n=Number(v);if(Number.isFinite(n))return n>1e12?n:n*1000;const d=Date.parse(String(v));return Number.isFinite(d)?d:NaN}
  function sequenceSpeed(frames){const speeds=[];for(let i=1;i<frames.length;i++){const a=frames[i-1],b=frames[i];if(String(a?.sequenceId||'')!==String(b?.sequenceId||''))continue;const dt=(timestampMs(b?.capturedAt)-timestampMs(a?.capturedAt))/1000,d=distanceMeters(a,b);if(Number.isFinite(dt)&&dt>.15&&dt<90&&Number.isFinite(d)&&d<250)speeds.push(d/dt)}return median(speeds)}
  function sampleFrames(frames,count=SAMPLE_POINTS){const good=frames.filter(hasCoords);if(good.length<=count)return good;const out=[];for(let i=0;i<count;i++)out.push(good[Math.round(i*(good.length-1)/(count-1))]);return out}
  function cacheKey(sequenceId,frames){const sample=sampleFrames(frames,1)[0]||frames[0]||{},lat=Number(sample.lat),lng=Number(sample.lng);return`${CACHE_PREFIX}${String(sequenceId||'unknown')}:${Number.isFinite(lat)?lat.toFixed(3):'x'}:${Number.isFinite(lng)?lng.toFixed(3):'x'}`}
  function readCache(key){try{const row=JSON.parse(localStorage.getItem(key)||'null');if(row&&Date.now()-Number(row.savedAt||0)<CACHE_TTL_MS)return row.value||null}catch{}return null}
  function writeCache(key,value){try{localStorage.setItem(key,JSON.stringify({savedAt:Date.now(),value}))}catch{}}
  function onFootRatio(frames){let yes=0,known=0;for(const f of frames){const v=f?.onFoot??f?.on_foot;if(v===true||v===1||v==='1'){yes++;known++}else if(v===false||v===0||v==='0')known++}return known?yes/known:NaN}
  function scoreFromOsm(osm,speed,onFoot){const c=osm?.counts||{...(osm?.highway||{}),...(osm?.railway||{})},scores={CAR:0,WALK:0,TRAIL:0,BIKE:0,TRAIN:0,UNKNOWN:0};scores.TRAIN+=(c.rail||0)*7+(c.tram||0)*6+(c.subway||0)*6+(c.light_rail||0)*6;scores.WALK+=(c.footway||0)*5+(c.pedestrian||0)*5+(c.steps||0)*6;scores.TRAIL+=(c.path||0)*5+(c.track||0)*5+(c.bridleway||0)*5;scores.BIKE+=(c.cycleway||0)*6;scores.CAR+=(c.motorway||0)*7+(c.trunk||0)*6+(c.primary||0)*5+(c.secondary||0)*4+(c.tertiary||0)*3+(c.residential||0)*2+(c.service||0)*1.5;if(Number.isFinite(onFoot)){if(onFoot>=.55){scores.WALK+=6;scores.TRAIL+=3;scores.CAR-=4}else if(onFoot<=.15)scores.CAR+=1}if(Number.isFinite(speed)){if(speed>=10)scores.CAR+=6;else if(speed>=5.5){scores.CAR+=4;scores.BIKE+=2}else if(speed>=2.2){scores.BIKE+=3;scores.WALK+=1}else{scores.WALK+=3;scores.TRAIL+=2}}if(scores.TRAIN>0&&scores.TRAIN>=Math.max(scores.CAR,scores.WALK,scores.TRAIL,scores.BIKE)*.85)return{type:'TRAIN',scores};const order=['WALK','TRAIL','BIKE','CAR','TRAIN'];let best='UNKNOWN',bestScore=0;for(const t of order)if(scores[t]>bestScore){best=t;bestScore=scores[t]}if(bestScore<2)return{type:'UNKNOWN',scores};if(best==='TRAIL'&&Number.isFinite(speed)&&speed>8&&scores.CAR>=scores.TRAIL*.55)best='CAR';if(best==='WALK'&&(c.path||c.track||c.bridleway)&&!(c.footway||c.pedestrian||c.steps))best='TRAIL';return{type:best,scores}}
  async function osmForSequence(sequenceId,frames){const key=cacheKey(sequenceId,frames),cached=readCache(key);if(cached){state.cacheHits++;return cached}const points=sampleFrames(frames).map(f=>`${Number(f.lat).toFixed(6)},${Number(f.lng).toFixed(6)}`).join(';');if(!points)return{counts:{},ways:[],primaryType:'UNKNOWN',cached:false};state.requests++;try{const r=await fetch(`/api/imagery?mode=osm-transport&points=${encodeURIComponent(points)}`,{cache:'force-cache'}),j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||`OSM ${r.status}`);writeCache(key,j);return j}catch(error){state.errors++;return{counts:{},ways:[],primaryType:'UNKNOWN',error:String(error?.message||error)}}}
  function groups(frames){const m=new Map();for(const f of frames){const id=String(f?.sequenceId||'unknown');if(!m.has(id))m.set(id,[]);m.get(id).push(f)}return m}
  function routeMajority(frames){const weights={CAR:0,WALK:0,TRAIL:0,BIKE:0,TRAIN:0,UNKNOWN:0};for(const f of frames){const t=TYPES.has(f?.transportType)?f.transportType:'UNKNOWN';weights[t]++}let best='UNKNOWN',n=0;for(const[t,v]of Object.entries(weights))if(t!=='UNKNOWN'&&v>n){best=t;n=v}return best}
  function applyHysteresis(frames,sequenceModes){let active='UNKNOWN',candidate='UNKNOWN',run=0;for(const f of frames){const desired=sequenceModes[String(f?.sequenceId||'unknown')]||'UNKNOWN';if(active==='UNKNOWN'){active=desired;candidate=desired;run=TRANSITION_CONFIRM_FRAMES}else if(desired===active){candidate=desired;run=0}else if(desired===candidate){run++;if(run>=TRANSITION_CONFIRM_FRAMES){active=desired;run=0}}else{candidate=desired;run=1}f.transportType=TYPES.has(active)?active:'UNKNOWN'}}
  function publishClassification(list,transportMode,sequenceModes,details,profile,profileSource){applyHysteresis(list,sequenceModes);setRouteProfile(profile,profileSource,transportMode,details);const last={at:new Date().toISOString(),transportMode,journeyProfile:profile,profileSource,sequenceModes,details};Object.assign(state,{transportMode,sequenceModes,details,last});try{window.dispatchEvent(new CustomEvent('journey-transport-classified',{detail:last}))}catch{}return last}

  async function classifyRoute(frames){const list=Array.isArray(frames)?frames:[];if(!list.length)return{transportMode:'UNKNOWN',journeyProfile:currentProfile(),sequenceModes:{}};const route=selectedRoute(),hint=profileFromHints(route,list);if(hint.profile!=='AUTO'&&hint.profile!=='UNKNOWN'){const type=PROFILE_TYPE[hint.profile]||'UNKNOWN',sequenceModes={},details={};for(const[sequenceId,seqFrames]of groups(list)){sequenceModes[sequenceId]=type;details[sequenceId]={type,explicitProfile:hint.profile,profileSource:hint.source,speedMps:Number.isFinite(sequenceSpeed(seqFrames))?Math.round(sequenceSpeed(seqFrames)*100)/100:null,onFootRatio:Number.isFinite(onFootRatio(seqFrames))?Math.round(onFootRatio(seqFrames)*100)/100:null,osmCounts:{},scores:{[type]:999},cache:true,error:null}}return publishClassification(list,type,sequenceModes,details,hint.profile,hint.source)}const sequenceModes={},details={};for(const[sequenceId,seqFrames]of groups(list)){const osm=await osmForSequence(sequenceId,seqFrames),speed=sequenceSpeed(seqFrames),foot=onFootRatio(seqFrames),decision=scoreFromOsm(osm,speed,foot);sequenceModes[sequenceId]=decision.type;details[sequenceId]={type:decision.type,speedMps:Number.isFinite(speed)?Math.round(speed*100)/100:null,onFootRatio:Number.isFinite(foot)?Math.round(foot*100)/100:null,osmCounts:osm?.counts||{...(osm?.highway||{}),...(osm?.railway||{})},scores:decision.scores,cache:!!osm?.cached,error:osm?.error||null}}applyHysteresis(list,sequenceModes);const transportMode=routeMajority(list),profile=TYPE_PROFILE[transportMode]||'UNKNOWN';return publishClassification(list,transportMode,sequenceModes,details,profile,'transport-classifier')}

  window.JourneyRouteProfiles={version:VERSION,profiles:[...PROFILES],typeToProfile:{...TYPE_PROFILE},profileToType:{...PROFILE_TYPE},current:currentProfile,is:isProfile,set:(profile,source='manual')=>setRouteProfile(profile,source,PROFILE_TYPE[normalizeProfile(profile)]||'UNKNOWN')};
  window.JourneyTransportClassifier={version:VERSION,classifyRoute,state:()=>JSON.parse(JSON.stringify(state)),profile:currentProfile,isProfile,test:{scoreFromOsm,sequenceSpeed,applyHysteresis,profileFromHints}};
})();
