/* Streetview Journey v0.1.31 Phase 1.6.3 Pedestrian Vanishing Point */
(()=>{
  'use strict';
  const VERSION='0.1.31';
  const MOTION_WAIT_COMPAT_MS=360;
  const MIN_AXIS_CONF=.16;
  const FULL_MIN_CONF=.20;
  const CAL_MIN_CONF=.18;
  const POSE_COMPENSATION=.78;
  const ANCHOR_WINDOW_RADIUS=2;
  const FULL_SAMPLE_COUNT=8;
  const BOOTSTRAP_WAIT_MS=760;
  const PREFLIGHT_MIN_ANALYSES=3;
  const PREFLIGHT_TARGET_ANALYSES=5;
  const PREFLIGHT_POOL_SIZE=11;
  const PREFLIGHT_IMAGE_TIMEOUT_MS=2800;
  const PREFLIGHT_RESULT_TIMEOUT_MS=2200;
  const OVERRIDE_MAX_ALIGNMENT_REGRESSION=12;
  const OVERRIDE_HARD_ALIGNMENT=28;
  const OVERRIDE_MIN_VISUAL_GAIN=.08;
  const OVERRIDE_ESCAPE_OUTSIDE=.65;
  const LOCAL_CROP_RADIUS=5;
  const LOCAL_FULL_RADIUS=9;
  const LOCAL_SMOOTH_RADIUS=2;
  const LOCAL_MAX_BIAS=.18;
  const LOCAL_MIN_CONF=.20;
  const PEDESTRIAN_STEP_MAX=1.85;
  const PEDESTRIAN_BIAS_MAX=.34;
  const PEDESTRIAN_MIN_LOCAL_CONF=.14;
  const NativeWorker=window.Worker;
  const NativePost=NativeWorker?.prototype?.postMessage;
  const nativeSetTimeout=window.setTimeout.bind(window);
  const nativeDrawImage=CanvasRenderingContext2D.prototype.drawImage;
  const axisByFrame=new Map(),fullAxisByFrame=new Map(),urlToFrame=new Map(),metaAnchorCache=new Map(),bootstrapImages=new Map(),preflightPending=new Map();
  let routeFrames=[],routeSelection=null,generation=0,axisWorker=null,axisErrors=0,lastAxis=null,lastFullAxis=null,diagValue;
  let calibrationCache=null,bootstrapPromise=Promise.resolve(),bootstrapResolve=null,bootstrapGeneration=0,preflightSeq=0;

  const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
  const rad=v=>v*Math.PI/180,deg=v=>v*180/Math.PI;
  const angle=(a,b)=>Number.isFinite(a)&&Number.isFinite(b)?((b-a+540)%360)-180:0;
  const normalizeURL=value=>{try{const u=new URL(String(value||''),location.href);u.searchParams.delete('analysis');u.searchParams.delete('axisv');return u.href;}catch{return String(value||'').replace(/([?&])(analysis|axisv)=[^&]+(&|$)/g,'$1').replace(/[?&]$/,'');}};
  const weightedMedian=items=>{if(!items.length)return 0;const s=[...items].sort((a,b)=>a.v-b.v),total=s.reduce((n,x)=>n+x.w,0);let acc=0;for(const item of s){acc+=item.w;if(acc>=total*.5)return item.v;}return s[s.length-1].v;};
  const median=a=>{const s=a.filter(Number.isFinite).sort((x,y)=>x-y);if(!s.length)return 0;const m=s.length>>1;return s.length&1?s[m]:(s[m-1]+s[m])*.5;};
  const hasCoords=f=>Number.isFinite(f?.lat)&&Number.isFinite(f?.lng);
  function distanceMeters(a,b){if(!hasCoords(a)||!hasCoords(b))return Infinity;const p1=rad(a.lat),p2=rad(b.lat),dp=p2-p1,dl=rad(b.lng-a.lng),s=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return 12742000*Math.atan2(Math.sqrt(s),Math.sqrt(Math.max(0,1-s)));}
  function bearing(a,b){if(!hasCoords(a)||!hasCoords(b))return null;const p1=rad(a.lat),p2=rad(b.lat),dl=rad(b.lng-a.lng),y=Math.sin(dl)*Math.cos(p2),x=Math.cos(p1)*Math.sin(p2)-Math.sin(p1)*Math.cos(p2)*Math.cos(dl);return(deg(Math.atan2(y,x))+360)%360;}
  function pedestrianProfile(i){
    const steps=[];for(let k=Math.max(1,i-5);k<=Math.min(routeFrames.length-1,i+6);k++){const a=routeFrames[k-1],b=routeFrames[k];if(String(a?.sequenceId||'')!==String(b?.sequenceId||''))continue;const d=distanceMeters(a,b);if(Number.isFinite(d)&&d>.05&&d<15)steps.push(d)}
    const step=steps.length?median(steps):Infinity;return{pedestrian:steps.length>=3&&step<=PEDESTRIAN_STEP_MAX,medianStep:step,samples:steps.length};
  }
  function robustPedestrianBearing(i){
    const c=routeFrames[i];if(!c)return null;const seq=String(c.sequenceId||'');let sx=0,sy=0,sw=0;
    for(let s=1;s<=18&&i+s<routeFrames.length;s++){const n=routeFrames[i+s];if(String(n?.sequenceId||'')!==seq)break;const d=distanceMeters(c,n);if(Number.isFinite(d)&&d>=3.2){const br=bearing(c,n),w=Math.min(d,14)/(1+.15*(s-1));sx+=Math.cos(rad(br))*w;sy+=Math.sin(rad(br))*w;sw+=w;if(d>=10)break}}
    for(let s=1;s<=14&&i-s>=0;s++){const p=routeFrames[i-s];if(String(p?.sequenceId||'')!==seq)break;const d=distanceMeters(p,c);if(Number.isFinite(d)&&d>=3.0){const br=bearing(p,c),w=.72*Math.min(d,12)/(1+.18*(s-1));sx+=Math.cos(rad(br))*w;sy+=Math.sin(rad(br))*w;sw+=w;if(d>=9)break}}
    return sw?(deg(Math.atan2(sy,sx))+360)%360:null;
  }
  function travelBearing(i){
    const c=routeFrames[i];if(!c)return null;const profile=pedestrianProfile(i),ped=profile.pedestrian?robustPedestrianBearing(i):null;if(Number.isFinite(ped))return ped;let sx=0,sy=0,sw=0;
    for(let s=1;s<=8;s++)if(i+s<routeFrames.length){const n=routeFrames[i+s],d=distanceMeters(c,n);if(Number.isFinite(d)&&d>=.7){const br=bearing(c,n),w=Math.min(d,16)/(1+.38*(s-1));sx+=Math.cos(rad(br))*w;sy+=Math.sin(rad(br))*w;sw+=w;if(d>=20)break;}}
    for(let s=1;s<=5;s++)if(i-s>=0){const p=routeFrames[i-s],d=distanceMeters(p,c);if(Number.isFinite(d)&&d>=.7){const br=bearing(p,c),w=.62*Math.min(d,14)/(1+.48*(s-1));sx+=Math.cos(rad(br))*w;sy+=Math.sin(rad(br))*w;sw+=w;if(d>=18)break;}}
    if(sw)return(deg(Math.atan2(sy,sx))+360)%360;return Number.isFinite(c.heading)?c.heading:(Number.isFinite(c.projectionYaw)?c.projectionYaw:null);
  }
  const isSphere=f=>String(f?.projection||'').toUpperCase()==='SPHERE'||(Number.isFinite(f?.fieldOfView)&&f.fieldOfView>=180);
  function rawMetadataAnchor(i){const f=routeFrames[i];if(!f)return 50;const tr=travelBearing(i),ih=Number.isFinite(f.heading)?f.heading:f.projectionYaw;if(!Number.isFinite(tr)||!Number.isFinite(ih))return 50;const d=angle(ih,tr);return isSphere(f)?clamp(50+d/3.6,0,100):clamp(50+d/clamp(f.fieldOfView||100,45,170)*100,5,95);}
  function metadataAnchor(i){if(metaAnchorCache.has(i))return metaAnchorCache.get(i);const base=rawMetadataAnchor(i),sph=isSphere(routeFrames[i]);let sum=0,sw=0;for(let k=Math.max(0,i-ANCHOR_WINDOW_RADIUS);k<=Math.min(routeFrames.length-1,i+ANCHOR_WINDOW_RADIUS);k++){let v=rawMetadataAnchor(k);if(sph||isSphere(routeFrames[k])){let d=v-base;if(d>50)d-=100;if(d<-50)d+=100;v=base+d;}const dist=Math.abs(k-i),w=dist===0?4:dist===1?2:1;sum+=v*w;sw+=w;}let out=sw?sum/sw:base;if(sph)out=((out%100)+100)%100;else out=clamp(out,5,95);metaAnchorCache.set(i,out);return out;}
  function frameAspect(i){const f=routeFrames[i],w=Number(f?.width),h=Number(f?.height);if(w>0&&h>0)return w/h;if(isSphere(f))return 2;return 16/9;}
  function cropViewFraction(i){const aspect=frameAspect(i),canvasAspect=80/120;return clamp((aspect>canvasAspect?canvasAspect/aspect:1)/1.045,.10,1);}
  function wrappedBias(value,base,sphere){let d=value-base;if(sphere){while(d>.5)d-=1;while(d<-.5)d+=1;}return d;}
  function observationFromResult(r,space){if(!r||!Number.isFinite(r.frame)||!Number.isFinite(r.centerX)||!Number.isFinite(r.confidence))return null;const i=r.frame,sph=isSphere(routeFrames[i]);let x=r.centerX;if(space!=='full-image'){const a=metadataAnchor(i)/100,vf=cropViewFraction(i);x=a+vf*(r.centerX-a);}if(sph){while(x<-.5)x+=1;while(x>1.5)x-=1;}const cov=Number.isFinite(r.coverage)?r.coverage:.35,ir=Number.isFinite(r.inlierRatio)?r.inlierRatio:.35,err=Number.isFinite(r.medErr)?r.medErr:2.2;const agreement=Number.isFinite(r.modelAgreement)?r.modelAgreement:0,translation=Number.isFinite(r.translationDominance)?r.translationDominance:0;const agreeGain=String(r.kind||'').includes('disagree')?clamp(1-agreement*.75,.52,.82):1;const translationGain=translation>2.8?clamp(1-(translation-2.8)*.07,.58,1):1;const quality=clamp(r.confidence,0,1)*(.55+.45*clamp(cov,0,1))*(.65+.35*clamp(ir,0,1))*clamp(1.18-err/7,.45,1)*agreeGain*translationGain;const spaceWeight=space==='full-image'?1:.32,kindWeight=String(r.kind||'').startsWith('side-flow')?.72:1;return{frame:i,x,confidence:r.confidence,quality:quality*spaceWeight*kindWeight,space,kind:r.kind||'unknown',outside:x<0||x>1};}
  function allObservations(){const out=[];for(const r of fullAxisByFrame.values()){const o=observationFromResult(r,'full-image');if(o&&o.confidence>=FULL_MIN_CONF)out.push(o);}for(const r of axisByFrame.values()){const o=observationFromResult(r,'render-crop');if(o&&o.confidence>=MIN_AXIS_CONF)out.push(o);}return out;}
  function computeCalibration(){if(calibrationCache)return calibrationCache;const observations=allObservations(),items=[],full=[];let cropCount=0;for(const o of observations){const base=metadataAnchor(o.frame)/100,d=wrappedBias(o.x,base,isSphere(routeFrames[o.frame])),w=Math.max(.02,o.quality);items.push({v:d,w,o});if(o.space==='full-image')full.push(o);else cropCount++;}if(!items.length)return calibrationCache={bias:0,confidence:0,spread:1,samples:0,fullSamples:0,cropSamples:0,cameraYawBiasDeg:0,source:'metadata-only',outsideRate:0};let bias=weightedMedian(items),dev=items.map(x=>Math.abs(x.v-bias)),spread=median(dev),sumW=items.reduce((s,x)=>s+x.w,0),meanQ=sumW/items.length;const enough=full.length>=2||cropCount>=10,sampleStrength=clamp(full.length/4+cropCount/24,0,1),stability=clamp(1-spread/.18,.12,1),confidence=enough?clamp(sampleStrength*stability*(.55+.75*meanQ),0,1):clamp(sampleStrength*.35,0,.28);bias=clamp(bias,-.48,.48);const fov=median(routeFrames.map(f=>Number(f?.fieldOfView)).filter(v=>Number.isFinite(v)&&v>0&&v<200))||100;const outsideRate=observations.length?observations.filter(o=>o.outside).length/observations.length:0;calibrationCache={bias,confidence,spread,samples:items.length,fullSamples:full.length,cropSamples:cropCount,cameraYawBiasDeg:bias*fov,source:confidence>=CAL_MIN_CONF?(Math.abs(bias)>=.055?'visual-calibrated':'metadata-confirmed'):'metadata-only',outsideRate};return calibrationCache;}
  function localBiasRaw(frame,globalBias){const items=[],profile=pedestrianProfile(frame),limit=profile.pedestrian?PEDESTRIAN_BIAS_MAX:LOCAL_MAX_BIAS;for(const r of fullAxisByFrame.values()){const o=observationFromResult(r,'full-image');if(!o||o.confidence<FULL_MIN_CONF)continue;const d=Math.abs(o.frame-frame);if(d>LOCAL_FULL_RADIUS)continue;items.push({v:wrappedBias(o.x,metadataAnchor(o.frame)/100,isSphere(routeFrames[o.frame])),w:o.quality*(profile.pedestrian?2.15:1.7)/(1+d*.22)});}for(let k=Math.max(0,frame-LOCAL_CROP_RADIUS);k<=Math.min(routeFrames.length-2,frame+LOCAL_CROP_RADIUS);k++){const r=axisByFrame.get(k),o=observationFromResult(r,'render-crop');if(!o||o.confidence<MIN_AXIS_CONF)continue;const d=Math.abs(k-frame),distGain=d===0?1:d===1?.78:d===2?.58:.34;items.push({v:wrappedBias(o.x,metadataAnchor(k)/100,isSphere(routeFrames[k])),w:o.quality*distGain*(profile.pedestrian?1.35:1)});}if(!items.length)return{bias:globalBias,confidence:0,spread:1,samples:0};const b=weightedMedian(items),spread=median(items.map(x=>Math.abs(x.v-b))),weight=items.reduce((s,x)=>s+x.w,0),confidence=clamp(weight/(profile.pedestrian?1.35:1.7),0,1)*clamp(1-spread/.145,.18,1);return{bias:clamp(b,-limit,limit),confidence,spread,samples:items.length};}
  function localBias(frame,globalBias){const estimates=[],profile=pedestrianProfile(frame),limit=profile.pedestrian?PEDESTRIAN_BIAS_MAX:LOCAL_MAX_BIAS;for(let k=Math.max(0,frame-LOCAL_SMOOTH_RADIUS);k<=Math.min(routeFrames.length-1,frame+LOCAL_SMOOTH_RADIUS);k++){const e=localBiasRaw(k,globalBias);if(e.confidence<.05)continue;const d=Math.abs(k-frame),w=e.confidence*(d===0?1:d===1?.70:.42);estimates.push({v:e.bias,w,e});}if(!estimates.length)return{bias:globalBias,confidence:0,spread:1,samples:0};const bias=weightedMedian(estimates),confidence=clamp(estimates.reduce((s,x)=>s+x.w,0)/1.45,0,1),spread=median(estimates.map(x=>Math.abs(x.v-bias))),samples=estimates.reduce((s,x)=>s+x.e.samples,0);return{bias:clamp(bias,-limit,limit),confidence:confidence*clamp(1-spread/.11,.30,1),spread,samples};}
  function effectiveAnchor(frame){const profile=pedestrianProfile(frame),maxBias=profile.pedestrian?PEDESTRIAN_BIAS_MAX:LOCAL_MAX_BIAS,minLocal=profile.pedestrian?PEDESTRIAN_MIN_LOCAL_CONF:LOCAL_MIN_CONF,meta=metadataAnchor(frame)/100,sph=isSphere(routeFrames[frame]),cal=computeCalibration(),local=localBias(frame,cal.bias),endDistance=Math.min(frame,Math.max(0,routeFrames.length-1-frame));let bias=cal.bias,source=profile.pedestrian?'pedestrian-metadata-long-baseline':cal.source,localBlend=0;if(local.confidence>=minLocal){localBlend=profile.pedestrian?clamp((local.confidence-.07)/.52,.34,.94):clamp((local.confidence-.12)/.68,.18,.78);if(endDistance<=7&&local.confidence>=.24)localBlend=Math.max(localBlend,profile.pedestrian?.76:.56);const deltaLimit=profile.pedestrian?.24:.13,localDelta=clamp(local.bias-cal.bias,-deltaLimit,deltaLimit);bias=clamp(cal.bias+localDelta*localBlend,-maxBias,maxBias);source=Math.abs(localDelta*localBlend)>=.018?(profile.pedestrian?'pedestrian-visual-local':'visual-local-smoothed'):source;}let gain=clamp((cal.confidence-.07)/.40,0,1);if(cal.fullSamples>=3&&cal.spread<.10)gain=Math.max(gain,.78);if(local.confidence>.50)gain=Math.max(gain,profile.pedestrian?.94:.72);else if(local.confidence>.30)gain=Math.max(gain,profile.pedestrian?.84:.58);else if(profile.pedestrian&&local.confidence>=PEDESTRIAN_MIN_LOCAL_CONF)gain=Math.max(gain,.68);if(cal.confidence<CAL_MIN_CONF&&local.confidence<(profile.pedestrian?.20:.30)){bias=0;gain=0;source=profile.pedestrian?'pedestrian-metadata-long-baseline':'metadata-only';}if(cal.confidence<CAL_MIN_CONF&&local.confidence>=(profile.pedestrian?.20:.34)){bias=clamp(local.bias,-maxBias,maxBias);gain=Math.max(gain,profile.pedestrian?.80:.62);source=profile.pedestrian?'pedestrian-visual-local':'visual-local-smoothed';}let raw=meta+bias*gain;if(sph)raw=((raw%1)+1)%1;const outside=!sph&&(raw<0||raw>1),anchor=sph?raw:clamp(raw,.02,.98);if(outside)source='visual-edge-limit';return{anchor,meta,bias:bias*gain,confidence:Math.max(cal.confidence,local.confidence),source,outside,calibration:cal,localBias:local.bias,localConfidence:local.confidence,localBlend,pedestrian:profile.pedestrian,medianStep:profile.medianStep};}

  const state=window.__journeyTravelAxis={version:VERSION,mode:'phase1.6.3 pedestrian long-baseline + visual local vanishing point',worker:'starting',workerReady:false,preflightPolicy:{minAnalyses:PREFLIGHT_MIN_ANALYSES,targetAnalyses:PREFLIGHT_TARGET_ANALYSES,poolSize:PREFLIGHT_POOL_SIZE,distribution:'route-spread'},overridePolicy:{maxAlignmentRegressionDeg:OVERRIDE_MAX_ALIGNMENT_REGRESSION,hardAlignmentDeg:OVERRIDE_HARD_ALIGNMENT,minVisualGain:OVERRIDE_MIN_VISUAL_GAIN,escapeOutsideRate:OVERRIDE_ESCAPE_OUTSIDE},localHeadingPolicy:{cropRadius:LOCAL_CROP_RADIUS,fullRadius:LOCAL_FULL_RADIUS,smoothRadius:LOCAL_SMOOTH_RADIUS,maxBias:LOCAL_MAX_BIAS,pedestrianMaxBias:PEDESTRIAN_BIAS_MAX,pedestrianStepMax:PEDESTRIAN_STEP_MAX},generation:0,routeFrames:0,routeSelection:null,latest:null,latestFull:null,appliedFrame:null,appliedShift:0,visualShift:0,poseCompensation:0,metadataAnchor:50,effectiveAnchor:50,anchorSource:'metadata-only',appliedConfidence:0,localHeadingBias:0,localHeadingConfidence:0,outsideFov:false,pedestrian:false,medianStep:null,errors:0,anchorForFrame(frame,fallback){if(!Number.isFinite(frame)||frame<0||frame>=routeFrames.length)return Number.isFinite(fallback)?fallback:50;const e=effectiveAnchor(frame);return e.anchor*100;},waitForBootstrap(timeout=BOOTSTRAP_WAIT_MS){return Promise.race([bootstrapPromise,new Promise(r=>nativeSetTimeout(r,timeout))]);},snapshot(){const vals=[...axisByFrame.values()].filter(v=>Number.isFinite(v?.confidence)),good=vals.filter(v=>v.confidence>=MIN_AXIS_CONF),fullVals=[...fullAxisByFrame.values()].filter(v=>Number.isFinite(v?.confidence)),fullGood=fullVals.filter(v=>v.confidence>=FULL_MIN_CONF),cal=computeCalibration();return{version:VERSION,mode:this.mode,preflightPolicy:this.preflightPolicy,overridePolicy:this.overridePolicy,localHeadingPolicy:this.localHeadingPolicy,worker:this.worker,workerReady:this.workerReady,generation,routeFrames:routeFrames.length,routeSelection,results:vals.length,usable:good.length,averageConfidence:good.length?good.reduce((s,v)=>s+v.confidence,0)/good.length:0,fullResults:fullVals.length,fullUsable:fullGood.length,fullAverageConfidence:fullGood.length?fullGood.reduce((s,v)=>s+v.confidence,0)/fullGood.length:0,calibrationBias:cal.bias,calibrationConfidence:cal.confidence,calibrationSpread:cal.spread,calibrationSamples:cal.samples,fullCalibrationSamples:cal.fullSamples,cropCalibrationSamples:cal.cropSamples,cameraYawBiasDeg:cal.cameraYawBiasDeg,calibrationSource:cal.source,outsideObservationRate:cal.outsideRate,latest:lastAxis,latestFull:lastFullAxis,appliedFrame:this.appliedFrame,appliedShift:this.appliedShift,visualShift:this.visualShift,poseCompensation:this.poseCompensation,metadataAnchor:this.metadataAnchor,effectiveAnchor:this.effectiveAnchor,anchorSource:this.anchorSource,appliedConfidence:this.appliedConfidence,localHeadingBias:this.localHeadingBias,localHeadingConfidence:this.localHeadingConfidence,outsideFov:this.outsideFov,pedestrian:this.pedestrian,medianStep:this.medianStep,errors:axisErrors};}};

  try{Object.defineProperty(window,'__journeyDiagnostics',{configurable:true,get(){return diagValue;},set(v){diagValue=v;if(v&&typeof v==='object'){v.version=VERSION;v.travelAxis='phase1.6.3-pedestrian';v.travelAxisState=state;}}});}catch{}
  window.setTimeout=function(fn,ms,...args){const adjusted=Number(ms)===85?MOTION_WAIT_COMPAT_MS:ms;return nativeSetTimeout(fn,adjusted,...args);};
  function loadBootstrapImage(url){const key=normalizeURL(url);if(bootstrapImages.has(key))return bootstrapImages.get(key);let p;p=new Promise((resolve,reject)=>{const im=new Image();im.crossOrigin='anonymous';im.referrerPolicy='no-referrer';im.decoding='async';im.onload=()=>resolve(im);im.onerror=()=>{bootstrapImages.delete(key);reject(new Error('axis image load failed'));};im.src=`${key}${key.includes('?')?'&':'?'}axisv=${VERSION}`;});bootstrapImages.set(key,p);return p;}
  function grayPair(imA,imB){const ratioA=imA.naturalWidth/Math.max(1,imA.naturalHeight),ratioB=imB.naturalWidth/Math.max(1,imB.naturalHeight),ratio=(ratioA+ratioB)/2,w=160,h=clamp(Math.round(w/Math.max(.65,ratio)),72,128);const make=im=>{const c=document.createElement('canvas');c.width=w;c.height=h;const g=c.getContext('2d',{willReadFrequently:true});g.drawImage(im,0,0,w,h);const d=g.getImageData(0,0,w,h).data,o=new Uint8Array(w*h);for(let k=0,j=0;k<o.length;k++,j+=4)o[k]=Math.round(d[j]*.299+d[j+1]*.587+d[j+2]*.114);return o;};return{w,h,a:make(imA),b:make(imB)};}
  async function submitFullAxis(frame,gen){if(gen!==generation||!axisWorker||frame<0||frame>=routeFrames.length-1)return false;try{const[imA,imB]=await Promise.all([loadBootstrapImage(routeFrames[frame].url),loadBootstrapImage(routeFrames[frame+1].url)]);if(gen!==generation)return false;const g=grayPair(imA,imB),a=g.a.buffer,b=g.b.buffer;NativePost.call(axisWorker,{type:'axis',space:'full-image',frame,generation:gen,width:g.w,height:g.h,grayA:a,grayB:b,seed:frame*173+g.w*g.h},[a,b]);return true;}catch{return false;}}
  async function submitPreflightPair(frames,frame){if(!axisWorker||!Array.isArray(frames)||frame<0||frame>=frames.length-1)return null;let loaded=null;try{loaded=await Promise.race([Promise.all([loadBootstrapImage(frames[frame].url),loadBootstrapImage(frames[frame+1].url)]),new Promise(r=>nativeSetTimeout(()=>r(null),PREFLIGHT_IMAGE_TIMEOUT_MS))]);}catch{return null;}if(!loaded)return null;let g;try{g=grayPair(loaded[0],loaded[1]);}catch{return null;}return new Promise(resolve=>{const requestId=`pf-${++preflightSeq}`;let timer=null,settled=false;const done=r=>{if(settled)return;settled=true;if(timer)clearTimeout(timer);preflightPending.delete(requestId);resolve(r||null);};preflightPending.set(requestId,{resolve:done});try{const a=g.a.buffer,b=g.b.buffer;NativePost.call(axisWorker,{type:'axis',space:'preflight',requestId,frame,generation:-1,width:g.w,height:g.h,grayA:a,grayB:b,seed:frame*211+g.w*g.h},[a,b]);timer=nativeSetTimeout(()=>done(null),PREFLIGHT_RESULT_TIMEOUT_MS);}catch{done(null);}});}
  function preflightIndices(frames){const pairs=Math.max(0,(Array.isArray(frames)?frames.length:0)-1);if(!pairs)return[];const count=Math.min(PREFLIGHT_POOL_SIZE,pairs);if(count===1)return[0];const last=pairs-1,out=[];for(let k=0;k<count;k++)out.push(Math.round(last*k/(count-1)));return[...new Set(out)];}
  function distributedPrimary(frames){const pairs=Math.max(0,(Array.isArray(frames)?frames.length:0)-1),count=Math.min(PREFLIGHT_TARGET_ANALYSES,pairs);if(count<=0)return[];if(count===1)return[0];const last=pairs-1,out=[];for(let k=0;k<count;k++)out.push(Math.round(last*k/(count-1)));return[...new Set(out)];}
  function completedPreflight(r){return!!(r&&Number.isFinite(r.centerX)&&Number.isFinite(r.confidence)&&!['invalid','size','safe-error'].includes(String(r.kind||'')));}
  async function preflightCandidate(candidate){const frames=Array.isArray(candidate?.frames)?candidate.frames:[],pool=preflightIndices(frames);if(pool.length<PREFLIGHT_MIN_ANALYSES)return{sequenceId:candidate?.sequenceId||null,visualScore:0,confidence:0,outsideRate:1,samples:0,analysisSamples:0,usableSamples:0,attemptedPairs:0,established:false,required:PREFLIGHT_MIN_ANALYSES,sampledPairs:[]};const primary=distributedPrimary(frames),initial=await Promise.all(primary.map(i=>submitPreflightPair(frames,i))),completed=[];for(let n=0;n<initial.length;n++)if(completedPreflight(initial[n]))completed.push({...initial[n],preflightFrame:primary[n]});const remaining=pool.filter(i=>!primary.includes(i));let attempted=primary.length,cursor=0;while(completed.length<PREFLIGHT_MIN_ANALYSES&&cursor<remaining.length){const batch=remaining.slice(cursor,cursor+2);cursor+=batch.length;attempted+=batch.length;const retry=await Promise.all(batch.map(i=>submitPreflightPair(frames,i)));for(let n=0;n<retry.length;n++)if(completedPreflight(retry[n]))completed.push({...retry[n],preflightFrame:batch[n]});}const analyses=completed.slice(0,PREFLIGHT_TARGET_ANALYSES),established=analyses.length>=PREFLIGHT_MIN_ANALYSES,good=analyses.filter(r=>r.confidence>=MIN_AXIS_CONF),sampledPairs=analyses.map(r=>Number.isFinite(r.preflightFrame)?r.preflightFrame:r.frame);if(!established)return{sequenceId:candidate.sequenceId,visualScore:0,confidence:0,outsideRate:1,samples:analyses.length,analysisSamples:analyses.length,usableSamples:good.length,attemptedPairs:attempted,established:false,required:PREFLIGHT_MIN_ANALYSES,sampledPairs};if(!good.length)return{sequenceId:candidate.sequenceId,visualScore:.12,confidence:0,outsideRate:0,samples:analyses.length,analysisSamples:analyses.length,usableSamples:0,attemptedPairs:attempted,established:true,required:PREFLIGHT_MIN_ANALYSES,score:.0864,sampledPairs};let sw=0,sv=0,sc=0,out=0;for(const r of good){const outside=r.centerX<0||r.centerX>1;if(outside)out++;const margin=outside?0:clamp(1-Math.abs(r.centerX-.5)/.52,0,1),kindGain=String(r.kind||'').startsWith('side-flow')?.58:1,w=Math.max(.04,r.confidence)*(.55+.45*clamp(r.coverage||.35,0,1))*kindGain;sv+=margin*w;sc+=r.confidence*w;sw+=w;}const visualScore=sw?sv/sw:0,confidence=sw?sc/sw:0,outsideRate=out/good.length;return{sequenceId:candidate.sequenceId,visualScore,confidence,outsideRate,samples:analyses.length,analysisSamples:analyses.length,usableSamples:good.length,attemptedPairs:attempted,established:true,required:PREFLIGHT_MIN_ANALYSES,score:clamp(visualScore*.72+confidence*.28,0,1),sampledPairs};}
  function overrideAllowed(metaEval,e){if(e===metaEval)return{allowed:true,reason:'metadata-baseline',visualGain:0,alignmentRegressionDeg:0};const metaPf=metaEval.preflight,pf=e.preflight,metaAlign=Number(metaEval.candidate.alignmentErrorDeg),candAlign=Number(e.candidate.alignmentErrorDeg),visualGain=(Number(pf.score)||0)-(Number(metaPf.score)||0),regression=Number.isFinite(metaAlign)&&Number.isFinite(candAlign)?candAlign-metaAlign:0,escape=(metaPf.outsideRate||0)>=OVERRIDE_ESCAPE_OUTSIDE;if(Number.isFinite(candAlign)&&candAlign>OVERRIDE_HARD_ALIGNMENT&&!escape)return{allowed:false,reason:'hard-alignment',visualGain,alignmentRegressionDeg:regression};if(regression>OVERRIDE_MAX_ALIGNMENT_REGRESSION&&!escape)return{allowed:false,reason:'alignment-regression',visualGain,alignmentRegressionDeg:regression};if(visualGain<OVERRIDE_MIN_VISUAL_GAIN)return{allowed:false,reason:'insufficient-visual-gain',visualGain,alignmentRegressionDeg:regression};return{allowed:true,reason:escape?'metadata-outside-escape':'visual-gain',visualGain,alignmentRegressionDeg:regression};}
  async function chooseVisualCandidate(data){let candidates=Array.isArray(data?.candidateRoutes)?data.candidateRoutes.filter(c=>Array.isArray(c?.frames)&&c.frames.length>=2).slice(0,3):[];if(!candidates.length&&Array.isArray(data?.frames)&&data.frames.length>=2)candidates=[{sequenceId:data.sequenceId,anchorIndex:data.anchorIndex,direction:data.selection?.direction||'forward',score:Number.isFinite(data.selection?.score)?data.selection.score:1,alignmentErrorDeg:data.selection?.alignmentErrorDeg,forwardErrorDeg:data.selection?.forwardErrorDeg,reverseErrorDeg:data.selection?.reverseErrorDeg,proximityMeters:data.selection?.proximityMeters,frames:data.frames}];if(!candidates.length)return{candidate:null,evaluations:[],ready:false,reason:'no-candidate'};bootstrapImages.clear();const evaluations=await Promise.all(candidates.map(async candidate=>({candidate,preflight:await preflightCandidate(candidate)})));if(evaluations.some(e=>!e.preflight.established))return{candidate:null,evaluations,ready:false,reason:'insufficient-real-image-analyses'};const metaEval=evaluations.find(e=>String(e.candidate.sequenceId)===String(data.sequenceId))||[...evaluations].sort((a,b)=>(Number(b.candidate.score)||0)-(Number(a.candidate.score)||0))[0];let best=metaEval,bestScore=-Infinity;for(const e of evaluations){const guard=overrideAllowed(metaEval,e);Object.assign(e,{overrideAllowed:guard.allowed,overrideBlockReason:guard.reason,visualGain:guard.visualGain,alignmentRegressionDeg:guard.alignmentRegressionDeg});if(!guard.allowed)continue;const meta=clamp(Number(e.candidate.score)||0,0,1),pf=e.preflight,combined=pf.score*.68+meta*.32-(pf.outsideRate||0)*.18;if(combined>bestScore){bestScore=combined;best=e;}}return{candidate:best.candidate,evaluations,bestScore,ready:true,guardApplied:best!==metaEval,metadataCandidate:metaEval.candidate};}
  function bootstrapIndices(){const n=routeFrames.length-1;if(n<=0)return[];const raw=[0,1,2,Math.round(n*.20),Math.round(n*.40),Math.round(n*.60),Math.round(n*.80),n-1];return[...new Set(raw.map(v=>clamp(v,0,n-1)))].slice(0,FULL_SAMPLE_COUNT);}
  function beginBootstrap(gen){bootstrapGeneration=gen;bootstrapPromise=new Promise(resolve=>{bootstrapResolve=resolve;});const inds=bootstrapIndices();if(!inds.length){bootstrapResolve?.();return bootstrapPromise;}(async()=>{for(let p=0;p<inds.length;p+=2){if(gen!==generation)break;await Promise.allSettled(inds.slice(p,p+2).map(i=>submitFullAxis(i,gen)));await new Promise(r=>nativeSetTimeout(r,18));}nativeSetTimeout(()=>{if(gen===generation)bootstrapResolve?.();},120);})();nativeSetTimeout(()=>{if(gen===generation)bootstrapResolve?.();},BOOTSTRAP_WAIT_MS);return bootstrapPromise;}
  function maybeResolveBootstrap(){if(bootstrapGeneration!==generation||!bootstrapResolve)return;const good=[...fullAxisByFrame.values()].filter(r=>r.confidence>=FULL_MIN_CONF);if(good.length>=3){bootstrapResolve();bootstrapResolve=null;}}
  function resetRoute(frames,selection=null){generation++;routeFrames=Array.isArray(frames)?frames:[];routeSelection=selection&&typeof selection==='object'?JSON.parse(JSON.stringify(selection)):null;urlToFrame.clear();axisByFrame.clear();fullAxisByFrame.clear();metaAnchorCache.clear();calibrationCache=null;lastAxis=null;lastFullAxis=null;bootstrapImages.clear();for(let i=0;i<routeFrames.length;i++)if(routeFrames[i]?.url)urlToFrame.set(normalizeURL(routeFrames[i].url),i);Object.assign(state,{generation,routeFrames:routeFrames.length,routeSelection,latest:null,latestFull:null,appliedFrame:null,appliedShift:0,visualShift:0,poseCompensation:0,metadataAnchor:50,effectiveAnchor:50,anchorSource:'metadata-only',appliedConfidence:0,localHeadingBias:0,localHeadingConfidence:0,outsideFov:false,pedestrian:false,medianStep:null});try{NativePost?.call(axisWorker,{type:'reset',generation});}catch{}return beginBootstrap(generation);}
  if(window.Response?.prototype?.json){const nativeJson=Response.prototype.json;Response.prototype.json=async function(...args){const data=await nativeJson.apply(this,args),isImagery=String(this.url||'').includes('/api/imagery');try{if(isImagery&&Array.isArray(data?.frames)){const picked=await chooseVisualCandidate(data),visualPreflight=(picked?.evaluations||[]).map(e=>({sequenceId:e.candidate.sequenceId,metadataScore:e.candidate.score,visualScore:e.preflight.visualScore,confidence:e.preflight.confidence,outsideRate:e.preflight.outsideRate,samples:e.preflight.samples,analysisSamples:e.preflight.analysisSamples,usableSamples:e.preflight.usableSamples,attemptedPairs:e.preflight.attemptedPairs,sampledPairs:e.preflight.sampledPairs,established:e.preflight.established,required:e.preflight.required,overrideAllowed:e.overrideAllowed!==false,overrideBlockReason:e.overrideBlockReason||null,visualGain:Number.isFinite(e.visualGain)?e.visualGain:null,alignmentRegressionDeg:Number.isFinite(e.alignmentRegressionDeg)?e.alignmentRegressionDeg:null}));if(!picked?.ready||!picked?.candidate){data.selection={...(data.selection||{}),strategy:'visual-preflight-required',visualPreflightReady:false,visualPreflightPolicy:{minAnalyses:PREFLIGHT_MIN_ANALYSES,targetAnalyses:PREFLIGHT_TARGET_ANALYSES,distribution:'route-spread'},visualPreflight};data.preflightError={code:'VISUAL_PREFLIGHT_INCOMPLETE',message:'候補sequenceの実画像解析が3ペア以上成立しなかったため開始を停止しました',reason:picked?.reason||'unknown'};data.frames=[];return data;}const original=data.sequenceId,best=picked.candidate;data.sequenceId=best.sequenceId;data.anchorIndex=best.anchorIndex;data.frames=best.frames;data.selection={...(data.selection||{}),strategy:'direction+visual-guarded',visualPreflightReady:true,visualPreflightPolicy:{minAnalyses:PREFLIGHT_MIN_ANALYSES,targetAnalyses:PREFLIGHT_TARGET_ANALYSES,distribution:'route-spread'},visualOverridePolicy:{maxAlignmentRegressionDeg:OVERRIDE_MAX_ALIGNMENT_REGRESSION,hardAlignmentDeg:OVERRIDE_HARD_ALIGNMENT,minVisualGain:OVERRIDE_MIN_VISUAL_GAIN,escapeOutsideRate:OVERRIDE_ESCAPE_OUTSIDE},metadataChosenSequenceId:original,visualOverride:String(original)!==String(best.sequenceId),direction:best.direction||data.selection?.direction,alignmentErrorDeg:Number.isFinite(best.alignmentErrorDeg)?best.alignmentErrorDeg:data.selection?.alignmentErrorDeg,forwardErrorDeg:Number.isFinite(best.forwardErrorDeg)?best.forwardErrorDeg:data.selection?.forwardErrorDeg,reverseErrorDeg:Number.isFinite(best.reverseErrorDeg)?best.reverseErrorDeg:data.selection?.reverseErrorDeg,score:Number.isFinite(best.score)?best.score:data.selection?.score,proximityMeters:Number.isFinite(best.proximityMeters)?best.proximityMeters:data.selection?.proximityMeters,visualPreflight};const ready=resetRoute(data.frames,data.selection||null);await Promise.race([ready,new Promise(r=>nativeSetTimeout(r,BOOTSTRAP_WAIT_MS))]);}}catch(error){if(isImagery&&Array.isArray(data?.frames)){data.selection={...(data.selection||{}),strategy:'visual-preflight-required',visualPreflightReady:false,visualPreflightPolicy:{minAnalyses:PREFLIGHT_MIN_ANALYSES,targetAnalyses:PREFLIGHT_TARGET_ANALYSES,distribution:'route-spread'},visualPreflight:[]};data.preflightError={code:'VISUAL_PREFLIGHT_EXCEPTION',message:'Visual Preflightの実画像解析を完了できなかったため開始を停止しました',detail:String(error?.message||error)};data.frames=[];}}return data;};}
  function inferFrame(message){if(Number.isFinite(message?.frameIndex))return message.frameIndex;const w=Number(message?.width)||0,h=Number(message?.height)||0,seed=Number(message?.seed);if(Number.isFinite(seed)&&w&&h){const v=(seed-w*h)/31;if(Number.isFinite(v)&&Math.abs(v-Math.round(v))<.08)return Math.round(v);}return null;}
  if(NativeWorker&&NativePost){try{axisWorker=new NativeWorker(`/travel-axis-worker.js?v=${VERSION}`);axisWorker.addEventListener('message',event=>{const m=event.data||{};if(m.type==='axis-ready'){state.worker=m.engine||'tile-flow';state.workerReady=true;return;}if(m.type==='axis-boot-error'){axisErrors++;state.worker='fallback';state.workerReady=false;state.errors=axisErrors;bootstrapResolve?.();return;}if(m.type==='axis-error'){axisErrors++;state.errors=axisErrors;return;}if(m.type!=='axis-result'||!m.result)return;const r=m.result;if(r.space==='preflight'&&r.requestId){const p=preflightPending.get(r.requestId);if(p)p.resolve(r);return;}if(r.generation!==generation||!Number.isFinite(r.frame))return;calibrationCache=null;if(r.space==='full-image'){fullAxisByFrame.set(r.frame,r);lastFullAxis={...r};state.latestFull=lastFullAxis;maybeResolveBootstrap();}else{axisByFrame.set(r.frame,r);lastAxis={...r};state.latest=lastAxis;window.dispatchEvent(new CustomEvent('journey-travel-axis',{detail:{...r}}));}});axisWorker.addEventListener('error',()=>{axisErrors++;state.worker='fallback';state.workerReady=false;state.errors=axisErrors;bootstrapResolve?.();});NativeWorker.prototype.postMessage=function(message,transfer){if(this!==axisWorker&&message?.type==='analyze'&&message?.grayA instanceof ArrayBuffer&&message?.grayB instanceof ArrayBuffer){const frame=inferFrame(message);if(Number.isFinite(frame)&&frame>=0&&frame<10000){try{const a=message.grayA.slice(0),b=message.grayB.slice(0);NativePost.call(axisWorker,{type:'axis',space:'render-crop',frame,generation,width:message.width,height:message.height,grayA:a,grayB:b,seed:message.seed||frame*97},[a,b]);}catch{}}}return NativePost.call(this,message,transfer);};}catch{state.worker='fallback';state.workerReady=false;axisErrors++;state.errors=axisErrors;bootstrapResolve?.();}}else state.worker='unsupported';
  function frameForImage(image){if(!(image instanceof HTMLImageElement))return null;const key=normalizeURL(image.currentSrc||image.src),direct=urlToFrame.get(key);return Number.isFinite(direct)?direct:null;}
  function centeredCropX(canvasWidth,drawWidth,anchorPercent){if(!Number.isFinite(canvasWidth)||!Number.isFinite(drawWidth)||drawWidth<=0)return 0;if(drawWidth<=canvasWidth)return(canvasWidth-drawWidth)/2;const a=clamp(anchorPercent,0,100)/100;return clamp(canvasWidth*.5-drawWidth*a,canvasWidth-drawWidth,0);}
  CanvasRenderingContext2D.prototype.drawImage=function(image,...args){try{if(args.length===4&&this.canvas&&this.canvas.width>160&&this.canvas.height>220){const frame=frameForImage(image);if(Number.isFinite(frame)){let[x,y,w,h]=args;if([x,y,w,h].every(Number.isFinite)&&w>this.canvas.width*.92){const cw=this.canvas.width,ch=this.canvas.height,e=effectiveAnchor(frame),meta=e.meta*100,anchor=e.anchor*100,metaX=centeredCropX(cw,w,meta),baseX=centeredCropX(cw,w,anchor),m=typeof this.getTransform==='function'?this.getTransform():null,centerScreenShift=m?(m.a*(cw*.5)+m.c*(ch*.5)+m.e-cw*.5):0,mapX=m&&Number.isFinite(m.a)&&Math.abs(m.a)>.55?m.a:1,poseSource=clamp((-centerScreenShift*POSE_COMPENSATION)/mapX,-cw*.085,cw*.085),margin=Math.min(cw*.012,5),minX=cw-w+margin,maxX=-margin,safeMin=minX<=maxX?minX:cw-w,safeMax=minX<=maxX?maxX:0,correctedX=clamp(baseX+poseSource,safeMin,safeMax);x=correctedX;args=[x,y,w,h];Object.assign(state,{appliedFrame:frame,metadataAnchor:meta,effectiveAnchor:anchor,anchorSource:e.source,visualShift:e.anchor-e.meta,poseCompensation:(poseSource*mapX)/cw,appliedShift:(correctedX-metaX)*mapX/cw,appliedConfidence:e.confidence,localHeadingBias:e.localBias||0,localHeadingConfidence:e.localConfidence||0,outsideFov:e.outside,pedestrian:!!e.pedestrian,medianStep:Number.isFinite(e.medianStep)?e.medianStep:null});}}}}catch{}return nativeDrawImage.call(this,image,...args);};
  function updateDiagnosticLine(){const box=document.getElementById('journeyDiag');if(!box)return;const head=box.querySelector('.jd-head b');if(head)head.textContent='PHASE 1.6.3 DIAG';let line=document.getElementById('jdAxis');if(!line){line=document.createElement('div');line.id='jdAxis';const ref=document.getElementById('jdApplied')||document.getElementById('jdLine4');ref?.insertAdjacentElement('afterend',line);}const snap=state.snapshot(),dir=snap.appliedShift<-.008?'→':snap.appliedShift>.008?'←':'•',sel=snap.routeSelection,selText=String(sel?.strategy||'').startsWith('direction')?`${sel.direction==='reverse'?'REV':'FWD'} ${Number.isFinite(sel.alignmentErrorDeg)?Math.round(sel.alignmentErrorDeg)+'°':'—'}`:'FIX',calText=snap.calibrationConfidence>=CAL_MIN_CONF?`Cal ${Math.round(snap.cameraYawBiasDeg)}°/${Math.round(snap.calibrationConfidence*100)}%`:'Cal wait',locText=`Loc ${Math.round((snap.localHeadingBias||0)*100)}%/${Math.round((snap.localHeadingConfidence||0)*100)}%`,edge=snap.outsideFov?' EDGE':'',walk=snap.pedestrian?` WALK ${Number.isFinite(snap.medianStep)?snap.medianStep.toFixed(1):'?'}m`:'',pf=Array.isArray(sel?.visualPreflight)?sel.visualPreflight:[],pfOk=sel?.visualPreflightReady===true&&pf.length?Math.min(...pf.map(x=>Number(x.analysisSamples)||0)):0;line.textContent=`Axis ${state.workerReady?'Ready':'Fallback'}${walk} / Pre ${pfOk||0}p ${sel?.visualPreflightReady===false?'BLOCK':'OK'} / Sel ${selText} / Full ${snap.fullUsable}/${snap.fullResults} / ${calText} / ${locText} / ${snap.anchorSource}${edge} / Meta ${Math.round(snap.metadataAnchor||50)}→${Math.round(snap.effectiveAnchor||50)} / ${dir}${Math.round(Math.abs(snap.appliedShift||0)*100)}%`;line.style.color='rgba(255,215,145,.96)';if(diagValue&&typeof diagValue==='object'){diagValue.version=VERSION;diagValue.travelAxisSnapshot=snap;}}
  nativeSetTimeout(updateDiagnosticLine,120);setInterval(updateDiagnosticLine,250);
})();
