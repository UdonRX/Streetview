/* Streetview Journey v0.1.22 adaptive motion diagnostics */
(()=>{const $=id=>document.getElementById(id);function install(){const viewer=$('viewer');if(!viewer||$('journeyDiag'))return;const box=document.createElement('section');box.id='journeyDiag';box.hidden=true;box.setAttribute('aria-label','Journey Engine 診断値');box.innerHTML=`<div class="jd-head"><b>PHASE 1.3.2 DIAG</b><span id="jdVerdict">待機中</span></div><div id="jdLine0">Worker —</div><div id="jdLine1">Frame — / Source —</div><div id="jdLine2">Pts — → LK — → FB —</div><div id="jdLine3">RS — / Model — / Err —</div><div id="jdLine4">Flow 上/下 — / Engine Avg —</div>`;viewer.appendChild(box);const s=document.createElement('style');s.textContent=`#journeyDiag{position:absolute;z-index:8;left:14px;right:14px;bottom:calc(var(--safe-bottom) + 116px);padding:9px 11px;border:1px solid rgba(255,255,255,.20);border-radius:13px;background:rgba(4,8,12,.72);color:#fff;font:600 9px/1.42 -apple-system,BlinkMacSystemFont,"SF Pro Text",sans-serif;letter-spacing:.01em;backdrop-filter:blur(12px);-webkit-backdrop-filter:blur(12px);pointer-events:none;text-shadow:0 1px 4px rgba(0,0,0,.5)}#journeyDiag[hidden]{display:none!important}.jd-head{display:flex;justify-content:space-between;gap:10px;margin-bottom:4px}.jd-head b{font-size:9px;letter-spacing:.14em}.jd-head span{max-width:68%;text-align:right;color:#ffe598;font-weight:750}#journeyDiag>div:not(.jd-head){white-space:nowrap;overflow:hidden;text-overflow:ellipsis;color:rgba(255,255,255,.83)}#jdLine0{color:rgba(171,224,255,.95)!important}`;document.head.appendChild(s);}const pct=v=>Number.isFinite(v)?`${Math.round(v*100)}%`:'—',px=v=>Number.isFinite(v)?`${v.toFixed(2)}px`:'—';function verdict(d,p){if(d.worker==='fallback')return'Worker失敗→Far-field継続';if(!d.workerReady)return'Worker準備中';if(!p)return'解析ジョブ待機';if(p.source==='worker-error')return`解析エラー:${p.stage||'unknown'}`;if((p.corners||0)<8)return'特徴点不足';if((p.lkRatio||0)<.50)return'LK追跡を改善余地';if(p.source==='ransac'){if(p.modelKind==='background'&&(p.depthRatio||1)>1.35)return'遠景RANSACで視差回避';if((p.domainInlierRatio||0)>=.60&&(p.reprojection||0)<=1.0)return'RANSAC安定';if((p.domainInlierRatio||0)>=.45)return'RANSAC成立・外れ値あり';return'RANSAC弱め';}if((p.depthRatio||1)>1.35)return'視差強→背景モデル不足';if((p.fbRatio||0)<.40)return'FB追跡不足';return'RANSAC未成立';}function render(){const box=$('journeyDiag'),d=window.__journeyDiagnostics||{};if(!box)return;const active=$('startPanel')?.hidden;if(!active){box.hidden=true;return;}box.hidden=false;const p=d.lastWorkerPair;$('jdVerdict').textContent=verdict(d,p);const jobs=p?`${p.jobsCompleted??'—'}/${p.jobsReceived??'—'}`:'—';$('jdLine0').textContent=`Worker ${d.workerReady?'Ready':d.worker||'—'} / jsfeat / Jobs ${jobs}${p?.jobsErrored?` / errors ${p.jobsErrored}`:''}${d.workerError?` / ${d.workerError}`:''}`;$('jdLine1').textContent=`Frame ${p?`${p.frame+1}→${p.frame+2}`:'—'} / Source ${p?.source||'—'} / ${Number.isFinite(p?.ms)?`${p.ms.toFixed(1)}ms`:'—'} / ${p?.reason||'—'}`;$('jdLine2').textContent=`Pts ${p?.detected??'—'}→${p?.corners??'—'} / LK ${p?.lk??'—'} (${pct(p?.lkRatio)}) → FB ${p?.fb??'—'} (${pct(p?.fbRatio)}) / gate ${px(p?.fbThreshold)}`;const domain=`${p?.domainInliers??0}/${p?.domainTracks??0} (${pct(p?.domainInlierRatio)})`;$('jdLine3').textContent=`RS ${p?.inliers??0}/${p?.fb??0} (${pct(p?.inlierRatio)}) / ${p?.modelKind||'—'} ${domain} / Cov ${pct(p?.coverage)} / Err ${px(p?.reprojection)}`;$('jdLine4').textContent=`Flow 上/下 ${px(p?.upperFlow)} / ${px(p?.lowerFlow)} (${Number.isFinite(p?.depthRatio)?p.depthRatio.toFixed(2):'—'}x) / Engine Avg ${pct(d.averageConfidence||0)}`;}install();setInterval(render,350);console.info('Streetview Journey diagnostics v0.1.22');})();