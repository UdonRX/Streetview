(() => {
  'use strict';

  const DEST_KEY='streetview:phase3-destination';
  const ROUTE_KEY='streetview:journey-route';
  const TEST_KEY='streetview:kyoto-test-route';
  const TOKEN_KEY='streetview:mapillary-token';
  const CACHE_KEY='streetview:kyoto-mapillary-fixed-routes:v3';
  const GRAPH='https://graph.mapillary.com';
  const SOURCE='kyoto-test-routes',LINE='kyoto-test-route-lines',POINTS='kyoto-test-route-points',LABELS='kyoto-test-route-labels';

  const TARGET_FRAMES=200,MIN_FRAMES=170,MAX_FRAMES=220,INITIAL_FRAMES=32;
  const META_BATCH=12,PROBE_BATCH=10,PROBE_COUNT=42,REFINE_RADIUS=12;
  const LIGHT_FIELDS='id,sequence';
  const META_FIELDS='id,sequence,captured_at,computed_geometry,compass_angle,computed_compass_angle,thumb_256_url,thumb_1024_url,thumb_2048_url,is_pano';
  const PROFILE_BY_ID=Object.freeze({road:'ROAD',sidewalk:'SIDEWALK',mountain:'MOUNTAIN',rail:'RAIL'});

  const prepared=new Map(),resolving=new Map();
  const routes=[
    {id:'road',profile:'ROAD',label:'道路',name:'烏丸通',start:{lat:35.01039,lng:135.75943,name:'烏丸御池'},end:{lat:35.00367,lng:135.75937,name:'四条烏丸'},maxEndpointM:850,seedSequenceId:'zq8vy4bui6shkiv29w5fp9'},
    {id:'sidewalk',profile:'SIDEWALK',label:'歩道',name:'鴨川遊歩道',start:{lat:35.00902,lng:135.77168,name:'三条大橋東岸'},end:{lat:35.00368,lng:135.77155,name:'四条大橋東岸'},maxEndpointM:900,search:{stops:[0,.18,.36,.54,.72,.88,1],offsets:[0,18,-18,36,-36],cellRadius:12,limits:[8,5,3]}},
    {id:'mountain',profile:'MOUNTAIN',label:'山道',name:'伏見稲荷山',start:{lat:34.96720,lng:135.77322,name:'千本鳥居付近'},end:{lat:34.97055,lng:135.77920,name:'三ツ辻方面'},maxEndpointM:1300,search:{stops:[0,.2,.4,.6,.8,1],offsets:[0,24,-24,48,-48],cellRadius:14,limits:[8,5,3]}},
    {id:'rail',profile:'RAIL',label:'線路',name:'嵯峨野線',start:{lat:34.98758,lng:135.74245,name:'梅小路京都西'},end:{lat:34.99512,lng:135.74210,name:'丹波口'},maxEndpointM:1100,search:{stops:[0,.2,.4,.6,.8,1],offsets:[0,22,-22,44,-44],cellRadius:12,limits:[8,5,3]}}
  ];

  const $=id=>document.getElementById(id);
  const status=text=>{const el=$('testRouteStatus');if(el)el.textContent=text};
  const token=()=>{try{return localStorage.getItem(TOKEN_KEY)||''}catch{return''}};
  const sleep=ms=>new Promise(r=>setTimeout(r,ms));

  function loadCache(){try{const v=JSON.parse(localStorage.getItem(CACHE_KEY)||'{}');return v&&typeof v==='object'?v:{}}catch{return{}}}
  function saveCache(v){try{localStorage.setItem(CACHE_KEY,JSON.stringify(v))}catch{}}
  function validFixed(f){return !!f?.sequenceId&&Array.isArray(f.imageIds)&&f.imageIds.length>=MIN_FRAMES&&f.imageIds.length<=MAX_FRAMES}
  function distanceMeters(a,b){const r=Math.PI/180,p1=+a.lat*r,p2=+b.lat*r,dp=(+b.lat-+a.lat)*r,dl=(+b.lng-+a.lng)*r,s=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return 12742000*Math.atan2(Math.sqrt(s),Math.sqrt(Math.max(0,1-s)))}
  function coords(m){const c=m?.computed_geometry?.coordinates;return Array.isArray(c)&&c.length>=2?{lat:+c[1],lng:+c[0]}:null}
  function sequenceId(m){return String(m?.sequence?.id??m?.sequence??'').trim()}
  function interpolate(route,t){return{lat:route.start.lat+(route.end.lat-route.start.lat)*t,lng:route.start.lng+(route.end.lng-route.start.lng)*t}}
  function routeBearing(route){const r=Math.PI/180,p1=route.start.lat*r,p2=route.end.lat*r,dl=(route.end.lng-route.start.lng)*r,y=Math.sin(dl)*Math.cos(p2),x=Math.cos(p1)*Math.sin(p2)-Math.sin(p1)*Math.cos(p2)*Math.cos(dl);return(Math.atan2(y,x)*180/Math.PI+360)%360}
  function offsetPoint(p,meters,bearingDeg){if(!meters)return p;const br=bearingDeg*Math.PI/180;return{lat:p.lat+(Math.cos(br)*meters)/111320,lng:p.lng+(Math.sin(br)*meters)/(111320*Math.max(.2,Math.cos(p.lat*Math.PI/180)))}}
  function pointBbox(p,radiusM){const latPad=radiusM/111320,lngPad=radiusM/(111320*Math.max(.2,Math.cos(+p.lat*Math.PI/180)));return`${p.lng-lngPad},${p.lat-latPad},${p.lng+lngPad},${p.lat+latPad}`}

  async function mly(path){const t=token();if(!t)throw new Error('Mapillary Access Tokenが未設定です');const sep=path.includes('?')?'&':'?';const res=await fetch(`${GRAPH}${path}${sep}access_token=${encodeURIComponent(t)}`,{cache:'no-store'}),data=await res.json().catch(()=>({}));if(!res.ok||data?.error){const msg=data?.error?.message||`Mapillary API ${res.status}`,e=new Error(msg);e.reduceAmount=/reduce the amount of data/i.test(msg);throw e}return data}

  async function batchMeta(ids,fields=META_FIELDS,batch=META_BATCH){const out=new Map(),unique=[...new Set((ids||[]).map(String).filter(Boolean))];for(let i=0;i<unique.length;i+=batch){const part=unique.slice(i,i+batch);let data;try{data=await mly(`/images?image_ids=${encodeURIComponent(part.join(','))}&limit=${part.length}&fields=${encodeURIComponent(fields)}`)}catch(error){if(error?.reduceAmount&&part.length>3){const half=Math.ceil(part.length/2),a=await batchMeta(part.slice(0,half),fields,Math.max(3,Math.floor(batch/2))),b=await batchMeta(part.slice(half),fields,Math.max(3,Math.floor(batch/2)));for(const [k,v] of a)out.set(k,v);for(const [k,v] of b)out.set(k,v);continue}throw error}for(const row of data?.data||[])out.set(String(row.id),row);if(i+batch<unique.length)await sleep(18)}return out}
  async function orderedIds(seq){try{const data=await mly(`/image_ids?sequence_id=${encodeURIComponent(seq)}`);return(data?.data||[]).map(x=>String(x?.id??x)).filter(Boolean)}catch(error){if(error?.reduceAmount)return[];throw error}}

  function normalizeFrame(meta,seq,index,profile){const c=coords(meta),source=meta?.thumb_2048_url||meta?.thumb_1024_url||meta?.thumb_256_url;if(!c||!source)return null;const computed=+meta?.computed_compass_angle,exif=+meta?.compass_angle,heading=Number.isFinite(computed)?computed:(Number.isFinite(exif)?exif:null);return{id:String(meta.id),sequenceId:String(seq),sequenceIndex:index,lat:c.lat,lng:c.lng,heading,headingSource:Number.isFinite(computed)?'sfm-computed':'exif',exifHeading:Number.isFinite(exif)?exif:null,computedHeading:Number.isFinite(computed)?computed:null,projection:meta.is_pano?'SPHERE':'RECTILINEAR',fieldOfView:meta.is_pano?360:100,sourceUrl:source,raw2048Url:meta?.thumb_2048_url||null,raw1024Url:meta?.thumb_1024_url||null,raw256Url:meta?.thumb_256_url||null,url:source,provider:'Mapillary',capturedAt:meta.captured_at||null,journeyProfile:profile}}

  function sampleExact(segment,target=TARGET_FRAMES){if(segment.length<=target)return segment.slice();const out=[],used=new Set(),step=(segment.length-1)/(target-1);for(let i=0;i<target;i++){const id=segment[Math.round(i*step)];if(!used.has(id)){used.add(id);out.push(id)}}return out}
  function targetSegment(ids,startIndex,endIndex){if(!Array.isArray(ids)||ids.length<MIN_FRAMES||startIndex<0||endIndex<0)return null;const forward=endIndex>=startIndex;let lo=Math.min(startIndex,endIndex),hi=Math.max(startIndex,endIndex),goal=Math.min(TARGET_FRAMES,ids.length);while(hi-lo+1<goal&&(lo>0||hi<ids.length-1)){if(lo>0)lo--;if(hi-lo+1<goal&&hi<ids.length-1)hi++}let segment=ids.slice(lo,hi+1);if(segment.length>MAX_FRAMES||segment.length>TARGET_FRAMES)segment=sampleExact(segment,TARGET_FRAMES);if(!forward)segment.reverse();return segment.length>=MIN_FRAMES&&segment.length<=MAX_FRAMES?segment:null}

  async function tinySequenceSearch(point,radiusM,limits){let reduced=false,lastError=null;for(const limit of limits){try{const data=await mly(`/images?bbox=${encodeURIComponent(pointBbox(point,radiusM))}&limit=${limit}&fields=${encodeURIComponent(LIGHT_FIELDS)}`);return{rows:Array.isArray(data?.data)?data.data:[],reduced}}catch(error){lastError=error;if(!error?.reduceAmount)throw error;reduced=true;await sleep(22)}}return{rows:[],reduced:true,error:lastError}}
  async function collectSequenceHints(route){const cfg=route.search||{stops:[0,.25,.5,.75,1],offsets:[0],cellRadius:12,limits:[8,5,3]},perp=(routeBearing(route)+90)%360,hints=new Map();let reducedCells=0,okCells=0;for(let si=0;si<cfg.stops.length;si++){const base=interpolate(route,cfg.stops[si]);status(`${route.label}：軽量sequence探索 ${si+1}/${cfg.stops.length}…`);for(const offset of cfg.offsets){const p=offsetPoint(base,Math.abs(offset),offset<0?(perp+180)%360:perp),result=await tinySequenceSearch(p,cfg.cellRadius,cfg.limits);if(result.reduced)reducedCells++;if(result.rows.length)okCells++;for(const row of result.rows){const seq=sequenceId(row);if(!seq)continue;let h=hints.get(seq);if(!h)hints.set(seq,h={sequenceId:seq,stops:new Set(),hits:0,ids:new Set(),offsetPenalty:0});h.stops.add(si);h.hits++;if(row.id)h.ids.add(String(row.id));h.offsetPenalty+=Math.abs(offset)}await sleep(12)}}const candidates=[...hints.values()].map(h=>({...h,coverage:h.stops.size,score:h.stops.size*100+h.hits*5-h.offsetPenalty*.08})).sort((a,b)=>b.score-a.score);return{candidates,reducedCells,okCells}}

  async function locateEndpoints(route,seq,ids){if(ids.length<MIN_FRAMES)return null;const probeCount=Math.min(PROBE_COUNT,ids.length),probeIndexes=[];for(let i=0;i<probeCount;i++)probeIndexes.push(Math.round(i*(ids.length-1)/Math.max(1,probeCount-1)));const probeIds=[...new Set(probeIndexes.map(i=>ids[i]))],meta=await batchMeta(probeIds,'id,computed_geometry',PROBE_BATCH);let startIndex=-1,endIndex=-1,startD=Infinity,endD=Infinity;for(const i of probeIndexes){const c=coords(meta.get(ids[i]));if(!c)continue;const ds=distanceMeters(route.start,c),de=distanceMeters(route.end,c);if(ds<startD){startD=ds;startIndex=i}if(de<endD){endD=de;endIndex=i}}if(startIndex<0||endIndex<0)return null;const refine=new Set();for(const center of[startIndex,endIndex])for(let i=Math.max(0,center-REFINE_RADIUS);i<=Math.min(ids.length-1,center+REFINE_RADIUS);i++)refine.add(i);const refineMeta=await batchMeta([...refine].map(i=>ids[i]),'id,computed_geometry',PROBE_BATCH);startD=Infinity;endD=Infinity;for(const i of refine){const c=coords(refineMeta.get(ids[i]));if(!c)continue;const ds=distanceMeters(route.start,c),de=distanceMeters(route.end,c);if(ds<startD){startD=ds;startIndex=i}if(de<endD){endD=de;endIndex=i}}return{startIndex,endIndex,startD,endD}}
  async function fixedFromSequence(route,seq,source){const ids=await orderedIds(seq);if(ids.length<MIN_FRAMES)return null;const loc=await locateEndpoints(route,seq,ids);if(!loc)return null;const imageIds=targetSegment(ids,loc.startIndex,loc.endIndex);if(!imageIds)return null;return{sequenceId:seq,startImageId:imageIds[0],endImageId:imageIds[imageIds.length-1],anchorStartImageId:ids[loc.startIndex],anchorEndImageId:ids[loc.endIndex],imageIds,startDistanceMeters:loc.startD,endDistanceMeters:loc.endD,source}}
  async function resolveSeed(route){if(!route.seedSequenceId)return null;try{return await fixedFromSequence(route,route.seedSequenceId,'seed')}catch{return null}}

  async function resolveRoute(route){if(resolving.has(route.id))return resolving.get(route.id);const task=(async()=>{const cache=loadCache(),cached=cache[route.id];if(validFixed(cached)&&String(cached.journeyProfile||route.profile)===route.profile)return cached;let fixed=await resolveSeed(route);let discovery=null;if(!fixed){discovery=await collectSequenceHints(route);const candidates=discovery.candidates.filter(c=>c.coverage>=2).slice(0,12);let fallback=null;for(let i=0;i<candidates.length;i++){const c=candidates[i];status(`${route.label}：候補sequence精査 ${i+1}/${candidates.length}…`);let f=null;try{f=await fixedFromSequence(route,c.sequenceId,`tiny-corridor-${c.coverage}stops`)}catch(error){if(!error?.reduceAmount)console.warn('[KyotoTestRoutes] candidate failed',error)}if(!f)continue;const endpoint=Math.max(f.startDistanceMeters,f.endDistanceMeters),rank=endpoint-c.coverage*70;if(!fallback||rank<fallback.rank)fallback={rank,f};if(endpoint<=route.maxEndpointM){fixed=f;break}}if(!fixed&&fallback)fixed=fallback.f}if(!fixed){const suffix=discovery?.reducedCells?`（密集セル${discovery.reducedCells}件は自動縮小/スキップ済み）`:'';throw new Error(`${route.label}で約200枚続くsequenceを見つけられませんでした${suffix}`)}fixed={...fixed,routeId:route.id,label:route.label,journeyProfile:route.profile,targetFrames:TARGET_FRAMES,resolvedAt:new Date().toISOString(),discovery:{method:route.seedSequenceId?'seed':'tiny-sequence-probe',reducedCells:discovery?.reducedCells||0,okCells:discovery?.okCells||0}};const next=loadCache();next[route.id]=fixed;saveCache(next);return fixed})().finally(()=>resolving.delete(route.id));resolving.set(route.id,task);return task}

  async function buildPayload(route,fixed){const ids=fixed.imageIds,initialIds=ids.slice(0,Math.min(INITIAL_FRAMES,ids.length)),meta=await batchMeta(initialIds),frames=initialIds.map((id,i)=>normalizeFrame(meta.get(id),fixed.sequenceId,i,route.profile)).filter(Boolean);if(frames.length<2)throw new Error('固定ルートの初期画像URLを取得できませんでした');const loaded=new Set(frames.map(f=>String(f.id))),missing=initialIds.map((id,i)=>({id:String(id),sequenceId:String(fixed.sequenceId),sequenceIndex:i,journeyProfile:route.profile})).filter(r=>!loaded.has(r.id)),remaining=ids.slice(initialIds.length).map((id,i)=>({id:String(id),sequenceId:String(fixed.sequenceId),sequenceIndex:initialIds.length+i,journeyProfile:route.profile}));return{version:'0.4.13-profiled-fixed-200-test',source:'Mapillary',provider:'Mapillary',sequenceId:fixed.sequenceId,journeyProfile:route.profile,profileSource:'explicit-test-route',profileIsolation:true,destination:{...route.end,testRouteType:route.id,testRouteLabel:route.label,journeyProfile:route.profile},selection:{strategy:'fixed-test-sequence-200-low-data',direction:'forward',proximityMeters:fixed.startDistanceMeters,destinationDistanceMeters:fixed.endDistanceMeters,searchMode:fixed.source||'fixed-resolved',candidateCount:1,visualOverride:false,startImageId:fixed.startImageId,endImageId:fixed.endImageId,totalImageIds:ids.length,journeyProfile:route.profile},frames,streamPending:[...missing,...remaining],candidateRoutes:[],fixedTestRoute:{routeId:route.id,label:route.label,name:route.name,journeyProfile:route.profile,startImageId:fixed.startImageId,endImageId:fixed.endImageId,anchorStartImageId:fixed.anchorStartImageId||null,anchorEndImageId:fixed.anchorEndImageId||null,frameCount:ids.length,targetFrames:TARGET_FRAMES,resolvedAt:fixed.resolvedAt,discovery:fixed.discovery||null}}}

  async function prepare(route){const fixed=await resolveRoute(route);markButton(route.id,'ready',fixed.imageIds.length);return fixed}
  function markButton(id,state,count=null){const b=document.querySelector(`[data-route="${id}"]`);if(!b)return;b.dataset.fixedState=state;const badge=b.querySelector('em');if(badge)badge.textContent=state==='ready'?`約${count||TARGET_FRAMES}枚固定`:state==='loading'?'準備中':state==='error'?'再試行':'タップで固定'}
  function setSessionPreset(route,fixed){const preset={id:route.id,label:route.label,name:route.name,journeyProfile:route.profile,start:{...route.start},end:{...route.end},sequenceId:fixed?.sequenceId||null,startImageId:fixed?.startImageId||null,endImageId:fixed?.endImageId||null,frameCount:fixed?.imageIds?.length||null,registeredAt:'2026-08-31'};try{sessionStorage.setItem(TEST_KEY,JSON.stringify(preset));sessionStorage.setItem(DEST_KEY,JSON.stringify({lat:route.end.lat,lng:route.end.lng,name:route.end.name,testRouteType:route.id,testRouteLabel:route.label,journeyProfile:route.profile,startLat:route.start.lat,startLng:route.start.lng,startName:route.start.name}))}catch{}}
  async function startRoute(route,button){document.querySelectorAll('.test-route-button').forEach(x=>x.classList.toggle('is-active',x===button));if(!token()){status(`${route.label}：Mapillaryトークンを設定して。約200枚の固定区間を端末内に保存する。`);$('mapillarySettings')?.click();return}markButton(route.id,'loading');try{status(`${route.label}：${route.profile}専用ルートを約200枚で準備中…`);const fixed=await prepare(route);setSessionPreset(route,fixed);if(!prepared.has(route.id))prepared.set(route.id,buildPayload(route,fixed));const payload=await prepared.get(route.id);sessionStorage.setItem(ROUTE_KEY,JSON.stringify(payload));status(`${route.label}：Sequence ${fixed.sequenceId} / ${fixed.imageIds.length}枚固定。${route.profile}専用処理で開始`);location.href='/journey-map.html?autostart=1'}catch(e){prepared.delete(route.id);markButton(route.id,'error');status(`${route.label}：${e?.message||'固定ルート準備に失敗'}`)}}

  function geojson(){const features=[];for(const r of routes){features.push({type:'Feature',properties:{id:r.id,label:r.label,name:r.name,kind:'line'},geometry:{type:'LineString',coordinates:[[r.start.lng,r.start.lat],[r.end.lng,r.end.lat]]}});features.push({type:'Feature',properties:{id:r.id,label:`${r.label} 出発`,kind:'point'},geometry:{type:'Point',coordinates:[r.start.lng,r.start.lat]}});features.push({type:'Feature',properties:{id:r.id,label:`${r.label} 到着`,kind:'point'},geometry:{type:'Point',coordinates:[r.end.lng,r.end.lat]}})}return{type:'FeatureCollection',features}}
  function installMapOverlay(){const map=window.__streetviewCoverageMap;if(!map)return setTimeout(installMapOverlay,120);if(!map.loaded())return map.once('load',installMapOverlay);if(!map.getSource(SOURCE))map.addSource(SOURCE,{type:'geojson',data:geojson()});if(!map.getLayer(LINE))map.addLayer({id:LINE,type:'line',source:SOURCE,filter:['==',['get','kind'],'line'],paint:{'line-color':['match',['get','id'],'road','#ffb45d','sidewalk','#69e8ff','mountain','#9dff70','rail','#ff77d7','#fff'],'line-width':4,'line-opacity':.88,'line-dasharray':[1.4,1.1]}});if(!map.getLayer(POINTS))map.addLayer({id:POINTS,type:'circle',source:SOURCE,filter:['==',['get','kind'],'point'],paint:{'circle-radius':6,'circle-color':'#fff','circle-stroke-width':2.5,'circle-stroke-color':['match',['get','id'],'road','#ffb45d','sidewalk','#69e8ff','mountain','#9dff70','rail','#ff77d7','#07110f']}});if(!map.getLayer(LABELS))map.addLayer({id:LABELS,type:'symbol',source:SOURCE,filter:['==',['get','kind'],'line'],layout:{'symbol-placement':'line','text-field':['get','label'],'text-size':11,'text-allow-overlap':false},paint:{'text-color':'#fff','text-halo-color':'#07110f','text-halo-width':2}})}
  function installButtons(){const box=$('testRouteButtons');if(!box)return;box.innerHTML='';const cache=loadCache();for(const r of routes){const fixed=cache[r.id],b=document.createElement('button');b.type='button';b.className='test-route-button';b.dataset.route=r.id;b.innerHTML=`<strong>${r.label}</strong><span>${r.start.name} → ${r.end.name}</span><em>${validFixed(fixed)?`約${fixed.imageIds.length}枚固定`:'タップで固定'}</em>`;b.addEventListener('click',()=>startRoute(r,b));box.appendChild(b)}}
  function refreshStatusFromCache(){const cache=loadCache(),ready=routes.filter(r=>validFixed(cache[r.id]));if(!token()){status('Mapillaryトークン保存後、各ルートをタップすると約200枚の固定区間を作る。');return}if(!ready.length){status('ジャンル別プロファイルを分離済み。歩道は軽量sequence探索で約200枚を固定する。');return}status(`${ready.length}/4ルート固定済み。${ready.map(r=>`${r.label}:${cache[r.id].imageIds.length}枚`).join(' / ')}`)}
  function waitForTokenSave(){$('saveToken')?.addEventListener('click',()=>setTimeout(refreshStatusFromCache,500))}

  installButtons();installMapOverlay();waitForTokenSave();refreshStatusFromCache();
  window.__kyotoJourneyTestRoutes={routes,profiles:PROFILE_BY_ID,resolve:id=>{const r=routes.find(x=>x.id===id);return r?resolveRoute(r):null},cache:loadCache,clear:()=>{try{localStorage.removeItem(CACHE_KEY)}catch{}prepared.clear();installButtons();refreshStatusFromCache()}};
})();
